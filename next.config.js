/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains : ['purr.objects-us-east-1.dream.io','images.unsplash.com']
  },
}

module.exports = nextConfig
